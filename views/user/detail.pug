extends ../layout

block content
  main.flex-shrink-0
    .container
      .d-flex.justify-content-between.align-items-center.mt-5
        h2 #{user.name}
        form(method='post' action=`/${user.id}`)
          button(type='submit') Start sync
      .container
